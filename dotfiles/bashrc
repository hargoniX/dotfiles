# Generated by m4 at Fri Nov 30 19:06:18 CET 2001 from warning
#
# this is bashrc
#
# This file sources a default file (See below). If you want to 
# modify it, feel free to do so. 
# The default file should always be included since it contains 
# system relevant information.
#
. /mount/share/lib/skel/.bashrc_default


# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

alias dotdrop=~/dotfiles/dotdrop.sh --cfg=~/dotfiles/config.yaml
